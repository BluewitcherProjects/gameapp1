<x-app-layout>
    <x-slot name="header">
        {{ __('Withdraw Funds') }}
    </x-slot>

    <div class="py-6">
        <div class="max-w-xl mx-auto px-4 sm:px-6 lg:px-8">
            <x-card class="bg-primary-midnight/50 backdrop-blur-md overflow-hidden mb-6 relative">
                <div class="absolute inset-0 bg-gradient-to-r from-red-500/10 to-transparent pointer-events-none"></div>
                <div class="p-6 relative z-10 flex justify-between items-center">
                    <div>
                        <p class="text-metallic-silver text-sm uppercase mb-1">Withdrawable Balance</p>
                        <h2 class="text-3xl font-bold text-white">{{ price(user()->income) }}</h2>
                        <button type="button" onclick="withdrawAll({{ user()->income }})"
                            class="text-accent-gold text-xs font-bold hover:underline mt-1">Withdraw All</button>
                    </div>
                    <div class="text-right">
                        <p class="text-xs text-metallic-silver">Tax</p>
                        <p class="text-lg font-bold text-red-400">{{ setting('withdraw_charge') }}%</p>
                    </div>
                </div>
            </x-card>

            <x-card class="bg-primary-midnight/50 backdrop-blur-md overflow-hidden">
                <div class="p-6">
                    <form id="withdrawForm" method="POST" action="{{ route('user.withdraw.request') }}"
                        class="space-y-6">
                        @csrf
                        <input type="hidden" name="type" value="card">

                        <!-- Amount -->
                        <div>
                            <label class="block text-metallic-silver text-sm font-bold mb-2">Withdrawal Amount</label>
                            <div class="relative">
                                <span
                                    class="absolute left-4 top-1/2 transform -translate-y-1/2 text-metallic-silver font-bold">{{
                                    currency() }}</span>
                                <input type="number" name="amount" id="amountInput"
                                    placeholder="Minimum {{ price(setting('minimum_withdraw')) }}"
                                    class="w-full bg-primary-dark/50 border border-white/10 rounded-lg py-3 pl-10 pr-4 text-white focus:outline-none focus:border-red-500 transition-colors"
                                    required>
                            </div>
                        </div>

                        <!-- Bank Info (Read Only) -->
                        <div class="space-y-4">
                            <div>
                                <label class="block text-metallic-silver text-xs font-bold mb-1">Phone Number</label>
                                <input type="text" value="{{ user()->phone }}" name="mobile"
                                    class="w-full bg-white/5 border border-white/10 rounded-lg py-2 px-4 text-metallic-silver cursor-not-allowed"
                                    readonly>
                            </div>
                            <div>
                                <label class="block text-metallic-silver text-xs font-bold mb-1">Bank Name</label>
                                <input type="text" value="{{ user()->bank_name }}" name="back_name"
                                    class="w-full bg-white/5 border border-white/10 rounded-lg py-2 px-4 text-metallic-silver cursor-not-allowed"
                                    readonly>
                            </div>
                            <div>
                                <label class="block text-metallic-silver text-xs font-bold mb-1">Account Number</label>
                                <input type="text" value="{{ user()->gateway_number }}" name="bank_account"
                                    class="w-full bg-white/5 border border-white/10 rounded-lg py-2 px-4 text-metallic-silver cursor-not-allowed"
                                    readonly>
                            </div>

                            @if(empty(user()->bank_name) || empty(user()->gateway_number))
                            <div
                                class="bg-red-500/10 border border-red-500/20 rounded-lg p-3 flex items-center justify-between">
                                <span class="text-red-400 text-xs">Bank details missing.</span>
                                <a href="{{ route('user.bank') }}"
                                    class="text-white text-xs font-bold bg-red-500 px-3 py-1 rounded hover:bg-red-600 transition-colors">Add
                                    Bank</a>
                            </div>
                            @endif
                        </div>

                        <div class="bg-red-500/10 border border-red-500/20 rounded-lg p-4">
                            <h4 class="text-red-400 font-bold text-sm mb-2 flex items-center">
                                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                </svg>
                                Withdrawal Rules
                            </h4>
                            <ul class="list-disc list-inside text-xs text-metallic-silver space-y-1">
                                <li>Minimum withdrawal: {{ price(setting('minimum_withdraw')) }}</li>
                                <li>Processing time: 10 minutes - 24 hours (Bank dependent).</li>
                                <li>Withdrawals processed 24/7 (except bank holidays).</li>
                            </ul>
                        </div>

                        <button type="submit" id="withdrawBtn"
                            class="w-full bg-gradient-to-r from-red-600 to-red-700 hover:from-red-500 hover:to-red-600 text-white font-bold py-4 rounded-lg shadow-lg shadow-red-500/20 transition-all transform hover:-translate-y-1">
                            Confirm Withdrawal
                        </button>
                    </form>
                </div>
            </x-card>
        </div>
    </div>

    <script>
        function withdrawAll(amount) {
            document.getElementById('amountInput').value = amount;
        }

        document.getElementById('withdrawBtn').addEventListener('click', function (e) {
            const amount = parseFloat(document.getElementById('amountInput').value);
            // Basic client validation could go here
        });
    </script>
</x-app-layout>