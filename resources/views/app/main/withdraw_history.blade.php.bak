<x-app-layout>
    <x-slot name="header">
        {{ __('Withdraw History') }}
    </x-slot>

    <div class="py-6">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <x-card class="bg-primary-midnight/50 backdrop-blur-md overflow-hidden">
                <div class="p-6">
                    @php
                    $withdrawals = \App\Models\Withdrawal::where('user_id', auth()->id())->orderByDesc('id')->get();
                    @endphp

                    @if($withdrawals->count() > 0)
                    <div class="space-y-4">
                        @foreach($withdrawals as $withdrawal)
                        <div
                            class="bg-white/5 rounded-lg p-4 border border-white/5 hover:bg-white/10 transition-colors flex flex-col md:flex-row justify-between items-start md:items-center">
                            <div class="mb-2 md:mb-0">
                                <h4 class="text-white font-bold text-lg mb-1">Withdrawal Request</h4>
                                <p class="text-metallic-silver text-xs">{{
                                    \Carbon\Carbon::parse($withdrawal->created_at)->format('d M Y, h:i A') }}</p>
                                <p class="text-metallic-silver text-xs mt-1">Bank: {{ $withdrawal->bank_name ?? 'N/A' }}
                                    ({{ $withdrawal->account_no ?? 'N/A' }})</p>
                            </div>
                            <div
                                class="text-right w-full md:w-auto flex flex-row md:flex-col justify-between items-center md:items-end">
                                <span class="text-red-400 font-bold text-xl block">- {{ price($withdrawal->amount)
                                    }}</span>
                                <span
                                    class="inline-block px-2 py-1 rounded text-xs mt-1 border uppercase font-bold
                                            {{ $withdrawal->status == 'approved' ? 'bg-green-500/20 text-green-400 border-green-500/30' : 
                                               ($withdrawal->status == 'pending' ? 'bg-yellow-500/20 text-yellow-400 border-yellow-500/30' : 'bg-red-500/20 text-red-400 border-red-500/30') }}">
                                    {{ $withdrawal->status }}
                                </span>
                            </div>
                        </div>
                        @endforeach
                    </div>
                    @else
                    <div class="text-center py-12">
                        <div class="w-16 h-16 bg-white/5 rounded-full flex items-center justify-center mx-auto mb-4">
                            <svg class="w-8 h-8 text-metallic-silver" fill="none" stroke="currentColor"
                                viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z">
                                </path>
                            </svg>
                        </div>
                        <h3 class="text-lg font-medium text-white mb-2">No Withdrawals Found</h3>
                        <a href="{{ route('user.withdraw') }}" class="text-accent-gold hover:underline">Request
                            Withdrawal</a>
                    </div>
                    @endif
                </div>
            </x-card>
        </div>
    </div>
</x-app-layout>