<x-app-layout>
  <x-slot name="header">
    {{ __('Recharge Wallet') }}
  </x-slot>

  <div class="py-6">
    <div class="max-w-xl mx-auto px-4 sm:px-6 lg:px-8">
      <x-card class="bg-primary-midnight/50 backdrop-blur-md overflow-hidden mb-6 relative">
        <div class="absolute inset-0 bg-gradient-to-r from-accent-cyan/10 to-transparent pointer-events-none"></div>
        <div class="p-6 relative z-10 flex justify-between items-center">
          <div>
            <p class="text-metallic-silver text-sm uppercase mb-1">Current Balance</p>
            <h2 class="text-3xl font-bold text-white">{{ price(user()->balance) }}</h2>
          </div>
          <div class="w-12 h-12 bg-accent-cyan/20 rounded-full flex items-center justify-center text-accent-cyan">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6">
              </path>
            </svg>
          </div>
        </div>
      </x-card>

      <x-card class="bg-primary-midnight/50 backdrop-blur-md overflow-hidden">
        <div class="p-6">
          <form id="depositForm" method="POST" action="/services/deposit.php" class="space-y-6">
            @csrf
            <input type="hidden" name="user" value="{{ auth()->user()->phone }}">

            <!-- Amount Selection -->
            <div>
              <label class="block text-metallic-silver text-sm font-bold mb-2">Select Amount</label>
              <div class="grid grid-cols-3 gap-3 mb-4">
                @foreach([580, 1870, 4600, 5610, 14025, 25590, 65000] as $amt)
                <button type="button"
                  class="amount-btn bg-white/5 hover:bg-white/10 border border-white/10 text-white font-bold py-3 px-2 rounded-lg transition-all focus:ring-2 focus:ring-accent-gold"
                  onclick="selectAmount({{ $amt }})">
                  {{ currency() }}{{ $amt }}
                </button>
                @endforeach
              </div>

              <div class="relative">
                <span class="absolute left-4 top-1/2 transform -translate-y-1/2 text-metallic-silver font-bold">{{
                  currency() }}</span>
                <input type="number" name="amount" id="amountInput" placeholder="Enter custom amount"
                  class="w-full bg-primary-dark/50 border border-white/10 rounded-lg py-3 pl-10 pr-4 text-white focus:outline-none focus:border-accent-gold transition-colors"
                  required>
              </div>
            </div>

            <!-- Channel Selection -->
            <div>
              <label class="block text-metallic-silver text-sm font-bold mb-2">Payment Channel</label>
              <div class="relative">
                <select name="channel" id="channel"
                  class="w-full bg-primary-dark/50 border border-white/10 rounded-lg py-3 px-4 text-white focus:outline-none focus:border-accent-gold transition-colors appearance-none">
                  <option value="ch1">Fast Pay (UPI/Bank) - Recommended</option>
                  <option value="ch2">Secure Pay</option>
                </select>
                <div class="absolute inset-y-0 right-0 flex items-center px-4 pointer-events-none text-white">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </div>
              </div>
            </div>

            <!-- Instructions -->
            <div class="bg-yellow-500/10 border border-yellow-500/20 rounded-lg p-4">
              <h4 class="text-accent-gold font-bold text-sm mb-2 flex items-center">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                Important Notes
              </h4>
              <ul class="list-disc list-inside text-xs text-metallic-silver space-y-1">
                <li>Minimum recharge amount is ₹580.</li>
                <li>Ensure UTR number is submitted correctly after payment.</li>
                <li>Amounts are credited within 5-10 minutes.</li>
              </ul>
            </div>

            <button type="submit" id="rechargeBtn"
              class="w-full bg-accent-gold hover:bg-yellow-500 text-primary-dark font-bold py-4 rounded-lg shadow-lg shadow-accent-gold/20 transition-all transform hover:-translate-y-1">
              Proceed to Pay
            </button>
          </form>
        </div>
      </x-card>
    </div>
  </div>

  <script>
    function selectAmount(amount) {
      document.getElementById('amountInput').value = amount;
      // Highlight selected button logic could be added here
    }

    document.addEventListener('DOMContentLoaded', function () {
      const channelSelect = document.getElementById('channel');
      const form = document.getElementById('depositForm');

      channelSelect.addEventListener('change', function () {
        // Update action if channels have different endpoints
        // form.action = '/services/deposit.php'; 
      });

      document.getElementById('rechargeBtn').addEventListener('click', function (e) {
        const amount = parseFloat(document.getElementById('amountInput').value);
        if (isNaN(amount) || amount < 580) {
          e.preventDefault();
          alert('Minimum recharge amount is ₹580');
        }
      });
    });
  </script>
</x-app-layout>